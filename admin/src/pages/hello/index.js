import { Box } from "@components";

const Default = () => {
  return (
    <Box defFlex center sx={{ width: "100%", height: "100%" }}>
      HELLO
    </Box>
  );
};

export { Default as PageHello };
